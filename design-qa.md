# 个股分析右侧辅助分析设计 QA

## 对比目标

- 主布局参考：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-f413c2d3-57c4-4bea-b556-49b51cfef08f.png`
- 筹码分布参考：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-a5aa44b6-cae3-4419-9561-e30af6404708.png`
- 资金趋势内容参考：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-40722e6b-6b0c-4c39-8575-b9beeb528c2c.png`
- 实现页面：`http://127.0.0.1:3011/stock-analysis?symbol=300204.SZ&name=%E8%88%92%E6%B3%B0%E7%A5%9E`
- 实现顶部截图：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/one-trading-stock-analysis-qa/stock-analysis-top.png`
- 实现资金卡截图：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/one-trading-stock-analysis-qa/stock-analysis-fund-card.png`

## 视口与状态

- Codex 内置浏览器 CSS 视口：`1280 × 720`。
- 浏览器 `devicePixelRatio=2`；浏览器截图已归一为 `1280 × 720` 像素，因此按 CSS 像素判断实现布局。
- 主布局参考为 `3018 × 1954`，筹码参考为 `590 × 850`，资金趋势参考为 `1966 × 1118`；参考图尺寸与实现视口不同，因此按相同内容区域、层级和比例比较，不作错误的逐像素全屏比对。
- 状态：浅色主题、`300204.SZ 舒泰神`、167 个交易日、筹码已计算、单股资金流尚未缓存。
- 没有为了截图自动触发资金流 POST 或写入本地 Parquet；正常资金数据态由组件固定样本测试覆盖。

## 全视图比较

- 关键价位 K 线继续保持主视觉；右侧新增固定分析栏，与参考图两个红框的占位和纵向顺序一致。
- 页面内容上限从 `1280px` 扩展到 `1800px`，在宽屏使用原空白区域；当前 `1280px` 目标运行面仍保留可读的 K 线宽度。
- 右栏顺序为“筹码分布 -> 资金分析”；卡片边框、圆角、背景和间距沿用 one-trading 现有 token，没有引入另一套视觉语言。

## 聚焦区域比较

### 筹码分布

- 标题、120 日推算说明、均成本、中位、获利盘、套牢盘、90%/70% 成本区和横向筹码峰均与参考内容一致。
- 实际页面数据为：均成本 `24.08`、获利盘 `11.4%`、套牢盘 `88.6%`、90% 筹码 `21.48 ~ 27.28`、70% 筹码 `22.28 ~ 25.68`，与参考截图一致。
- 现价线、平均成本线、红/绿筹码语义及“非交易所官方筹码峰”说明完整保留。

### 资金分析

- 参考图中的核心信息被收敛为适合右栏的四项指标：最新主力、主力净占比、近 5 日累计、近 20 日累计，并配当日柱线与可见区间累计趋势。
- 当前真实运行面没有 `300204.SZ` 单股资金流缓存，因此正确展示按需获取空态；没有把空数据伪装成零值或自动外连。
- 固定样本测试覆盖有数据、显式获取并回读缓存、已有缓存刷新失败提示、24 日可见区间累计基线。

## 五项视觉检查

- 字体与排版：沿用项目现有无衬线正文与等宽数字；标题、说明、指标和图表轴标签层级清晰，无异常换行。
- 间距与布局节奏：主图和右栏间距 `16px`，右栏宽 `18.5rem`；卡片内部使用现有紧凑密度，未发现裁切或控件遮挡。
- 色彩与 token：使用 `border/background/text/bull/bear` 现有语义 token；筹码和资金流均遵循 A 股红流入、绿流出语义。
- 图表与资产质量：筹码和资金趋势使用 ECharts canvas；没有占位图、手绘 SVG、CSS 图案或低清位图替代数据图表。
- 文案与内容：明确标注“本地日K近似”“推算”“按需缓存”“东财公开资金流”，避免把近似筹码或缓存资金流误写成交易所官方数据。

## 交互与运行证据

- 直接 URL 进入后自动选中 `舒泰神 300204.SZ`。
- 筹码从加载态进入完整数据态。
- 资金卡空态、更新按钮和“获取资金流”按钮可见；自动化测试验证成功刷新和失败提示。
- 浏览器控制台没有本轮功能错误，仅有项目既存 React Router v7 future flag 提醒。
- 生产构建通过；相关 6 项前端测试通过。

## Findings

- 没有可执行的 P0 / P1 / P2 视觉或交互问题。
- P3：待用户首次获取真实单股资金流缓存后，可再对资金趋势图的真实峰值密度做一次视觉微调；这不阻塞当前功能。

## Comparison history

- 第 1 轮：主布局、筹码聚焦区和资金空态在同一目标运行面完成比较，未发现 P0 / P1 / P2，未触发视觉返工。
- 独立代码审查另发现并修复两项非视觉问题：已有缓存刷新失败静默、累计趋势包含不可见历史区间；修复后回归测试和复审均通过。

## Implementation checklist

- [x] 宽屏右侧辅助分析栏
- [x] 每只选中股票的筹码分布
- [x] 单股资金方向、指标和趋势
- [x] 显式按需获取与刷新失败提示
- [x] 窄屏卡片下移布局
- [x] 测试、构建、浏览器与控制台检查

final result: passed

---

# 行业分析大盘热力图设计 QA

## 对比目标

- 视觉真相：`/Users/simon/.codex/visualizations/2026/08/07/019fdb0d-0362-7ca2-879c-9608abaab43b/industry-heatmap-audit/eastmoney-reference.png`。
- 最终实现：`/Users/simon/.codex/visualizations/2026/08/07/019fdb0d-0362-7ca2-879c-9608abaab43b/industry-heatmap-audit/implemented-2048x1024-20260810.png`。
- 运行面：`http://127.0.0.1:3011/industry-analysis`，正式 3011/3018 进程与本地正式数据。

## 视口、像素与状态

- 参考图和最终实现截图均为 `2048 × 1024`；对照状态均为宽屏、两级行业/个股矩形树图、涨跌幅着色、流通市值面积口径。
- 实现保留 one-trading 既有浅色应用壳，只将热力图画布设为深色；没有复制东方财富品牌、指数头栏、市场筛选侧栏或时间回放控件。
- 正式数据状态：`2026-08-10`，行情 `5,207/5,540`，行业着色 `5,193/5,539`，90 个二级行业均有可着色个股；快照质量状态 `intraday_partial` 在界面翻译为“盘中快照”。

## 全视图与聚焦区域比较

- 与参考一致的核心层级为：行业标题区、按面积分配的行业组、组内个股块、红涨绿跌/灰平、涨跌幅文本和连续色阶图例。
- one-trading 在图上方保留来源时间、两种覆盖徽标和流通市值/成交额/等权切换，属于数据可信度与现有产品任务所需的适配。
- 参考图让热力图占满整屏；实现把它嵌入行业分析长页，宽屏画布约 1,440×660 CSS 像素，仍可同时辨认大市值个股、主要行业和全局涨跌密度。

## 五项视觉检查

- 字体与排版：沿用项目现有无衬线与数字字体；行业 13px 加粗、个股 11px，按面积自动省略，未出现文字溢出或覆盖。
- 间距与布局：行业间 2–3px 深色边界，个股间 1px；标题、徽标、口径按钮和图例在 1280 与 2048 宽度均未遮挡。
- 色彩与 token：遵循 A 股红涨绿跌，涨跌强度连续映射，0% 为中性灰；未使用西式绿涨红跌。
- 图表与资产质量：使用 ECharts canvas 真实数据绘制，没有低清位图、占位图、手工 SVG、CSS 假图或文字符号冒充图表。
- 文案与内容：行业均值、行情覆盖、来源时间、面积口径和 tooltip 单位明确；缺失报价不按平盘计数。

## 交互与运行证据

- 流通市值、成交额、等权三种面积口径均在正式浏览器实测切换，`aria-pressed` 状态正确并恢复默认流通市值。
- 组件测试覆盖行业选择和个股预览回调；实际页面继续复用现有 `StockPreviewDialog`。
- 正式浏览器控制台 error 为 0；前端全量 29 文件、111 测试通过，TypeScript/Vite 生产构建通过。

## Findings 与比较历史

- 第 1 轮发现 P1：现代空格式 HSL 未被 ECharts 正确解析，导致回退蓝紫默认色；同时 `leafDepth=1` 停在行业层，只显示钻取箭头。两项均已修复。
- 第 2 轮并排比较通过：红涨绿跌、行业分组、个股层级、面积差异和密度均与参考核心形态一致；没有剩余 P0 / P1 / P2 视觉问题。
- P3：当前未提供参考页的全屏、市场筛选或盘中回放；这些属于新功能，不阻塞本轮“类似东方财富热力图”的目标。

## Implementation checklist

- [x] 两级行业/个股矩形树图
- [x] A 股红涨绿跌与连续色阶
- [x] 流通市值、成交额、等权切换
- [x] 行情与行业着色覆盖披露
- [x] tooltip、行业选择与个股预览回调
- [x] 正式浏览器、同尺寸对照、控制台、测试与构建

final result: passed

---

# 个股日 K 44 项指标信号汇总设计 QA

## 对比目标

- 视觉真相：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-7161089d-caee-4551-8b3b-662aa1180ef4.png`。
- 浏览器实现截图：`/Users/simon/.codex/visualizations/2026/08/05/019fd0f2-2d5f-7250-ab6e-546203a0daf2/03-technical-signal-summary-implemented.png`。
- 聚焦区域对比：`/Users/simon/.codex/visualizations/2026/08/05/019fd0f2-2d5f-7250-ab6e-546203a0daf2/07-signal-summary-comparison.png`；上方为 go-stock 参考，下方为 one-trading 实现。

## 视口、像素与状态

- 实现 CSS 视口：`1280 × 720`，`devicePixelRatio=2`；Codex 内置浏览器截图归一为 `1280 × 720` 像素。
- 参考图：`3224 × 1902` 像素；实现图：`1280 × 720` 像素。
- 聚焦比较按各自信号汇总区域裁切并统一到 1600px 宽，不把原始截图密度、整页比例或周边页面差异当成组件偏差。
- 状态：浅色主题、`300502.SZ 新易盛`、半年日 K、最新交易日 `2026-08-05`、44 项全部展开。
- 正式 `3018` 后端当时因热重载等待长连接关闭而不响应；未重启或改配置。验证使用同一工作树和本地数据的隔离运行面 `3111/3118`。

## 全视图比较

- 信号汇总位于行情信息条下方、成交量/MACD/RSI/KDJ/BOLL/异动控制行上方，严格落在用户确认的位置。
- 展开高度约 117px，44 个标签在 1280px 视口换成三行，没有横向溢出、裁切、遮挡或覆盖 K 线。
- 收起后只保留标题、四色比例、四类计数和日期，不改变下面已有图表控制与 K 线交互。

## 聚焦区域比较

- 保留参考图的四个核心层次：标题/总数、四色比例条、看多/看空/震荡/中性计数、44 项彩色标签。
- one-trading 将计数收进标题行并增加“截至日期”和收起按钮，属于既有紧凑信息条的产品适配，不改变信号含义。
- 标签顺序与参考保持一致：`MA` 起、`ElderRay` 止；真实浏览器 DOM 确认共 44 个且无重复。

## 五项视觉检查

- 字体与排版：沿用项目无衬线标题和等宽指标标签；10–11px 密度与现有 K 线工具条一致，计数使用 tabular 数字。
- 间距与布局节奏：卡片圆角、边框、2.5 单位横向内边距和 1 单位标签间距沿用 one-trading token；面板与指标控制行之间保持清晰分区。
- 色彩与 token：看多/看空继续采用 A 股红/绿，看震荡用 warning，中性用 muted；每种颜色同时带文字名称和数字，不依赖单一颜色传义。
- 图像与资产质量：本功能是数据 UI，没有位图资产；展开图标复用项目既有 Lucide 图标，没有手绘 SVG、占位图或低清资源。
- 文案与内容：标题、44 项、截至日期及四类统计清晰；历史不足的指标使用虚线中性标签并通过 title 说明，不伪造有效计算。

## 交互与运行证据

- 浏览器实测展开态为 `aria-expanded=true` 且 44 个标签可见；收起态为 `aria-expanded=false` 且标签为 0；再次展开恢复 44 个。
- 十字光标从 `2026-05-18` 移到 `2026-06-18` 后，汇总日期由 `2026-05-18` 更新为 `2026-06-18`，四类计数从 `16 / 8 / 12 / 8` 更新为 `28 / 4 / 8 / 4`；指标颜色同步重算，且没有触发分时选中日期的副作用。
- 补强截图：`/Users/simon/.codex/visualizations/2026/08/05/019fd0f2-2d5f-7250-ab6e-546203a0daf2/08-hover-crosshair-fixed.png`。截图中十字线仍可见，横轴和纵轴均无白色空标签框，底部日期刻度保持无遮挡。
- 自动测试覆盖固定顺序、44 项唯一性、四类总和、选中 K 线日期和展开/收起。
- 新增指针回归覆盖悬停日期上传，以及全局/tooltip 两层轴标签关闭；相关 3 个测试文件共 7 项通过。
- 浏览器控制台没有本轮 error；只有项目既有 React Router v7 future flag warning。
- TypeScript/Vite 生产构建通过；相关 3 个测试文件共 11 项通过。

## Findings

- 没有可执行的 P0 / P1 / P2 视觉、可访问性或主交互问题。
- P3：后续若用户希望进一步节省纵向空间，可把默认状态改为收起；当前按“把这些指标都放在这里”的要求默认展开。

## Comparison history

- 第 1 轮：全视图与聚焦区域比较均未发现 P0 / P1 / P2；没有触发视觉返工。
- 第 2 轮：按用户反馈补测十字光标。首次真实复核发现 tooltip 自有轴标签仍产生白色遮片；补齐第二层配置后复核通过，悬停日期、统计和颜色联动成立，时间轴无遮挡。

## Implementation checklist

- [x] 44 项指标与固定顺序
- [x] 四类信号计数和比例条
- [x] 选中日期口径
- [x] 十字光标悬停日期联动
- [x] 移除横轴/纵轴空白标签框
- [x] 展开/收起与无障碍名称
- [x] 真实浏览器、控制台、测试和构建

final result: passed

---

# 行业分析热力图首屏与最大化设计 QA

## 对比目标

- 页面位置视觉真相：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-afe0b768-b81f-45ca-9851-4a741929374d.png`；用户红线指定热力图应位于标题下、KPI 之前。
- 普通热力图可读性参考：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-3ad69851-26d2-444f-969e-5eeefa445cd7.png`。
- 最大化悬浮参考：`/var/folders/g_/cx_qfcqn1nd45brx4p137xnm0000gn/T/codex-clipboard-b38a030f-5a51-458d-a928-fe0d49d36f1d.png`。
- 实现首屏：`/Users/simon/.codex/visualizations/2026/08/10/industry-heatmap-top-fullscreen/industry-heatmap-top-1382x1024.png`。
- 实现最大化：`/Users/simon/.codex/visualizations/2026/08/10/industry-heatmap-top-fullscreen/industry-heatmap-fullscreen-1382x1024.png`；悬浮同尺寸对照为 `industry-heatmap-fullscreen-hover-2048x1024.png`。

## 视口、像素与状态

- 页面位置参考原图 `2764×2048`，按 2 倍密度归一为 `1382×1024`；实现 CSS 视口和截图均为 `1382×1024`，归一图位于同一证据目录的 `source-page-top-normalized-1382x1024.png`。
- 最大化悬浮参考与实现均为 `2048×1024`；同为银行行业、农业银行悬浮、其他区域暗化状态。
- 额外检查 `1024×768` 较窄桌面：页面 `scrollWidth=clientWidth=1024`，热力图和最大化按钮没有横向溢出。
- 正式路由、主题和数据状态：`http://127.0.0.1:3011/industry-analysis`、浅色应用壳/深色图表、`2026-08-10`、行情 `5,207/5,540`、行业着色 `5,193/5,539`。

## 全视图与聚焦区域比较

- 首屏实现严格落在用户红线位置：`PageHeader` 后第一块即热力图，随后才是五项 KPI、资金流和行业矩阵；没有改动页面标题、数据或后续模块顺序。
- 普通模式在 `1382×1024` 中呈现完整 720px 高画布，主要行业、银行/半导体等大市值个股及平均涨跌均可直接阅读；最大化按钮位于面积口径和色阶右侧，不覆盖任何股票块。
- `2048×1024` 最大化悬浮与参考的行业分组、暗化聚焦、白色描边和深色 tooltip 结构一致；实现额外保留覆盖、数据时间、面积口径和明确退出入口，属于必要的产品控制层。

## 五项视觉检查

- 字体与排版：普通个股 12px/500、行业 14px/700；最大化个股 13px、行业 16px，tooltip 13–14px、21–23px 行高。大块股票名和涨跌清晰，小块继续按面积省略，避免文字互相覆盖。
- 间距与布局：普通画布使用 620/680/720px 响应式高度；最大化容器四周 16px、标题控制区约 68px，其余空间全部交给图表。行业 2–3px、个股 1px 分隔保持参考密度。
- 颜色与 token：继续使用 A 股红涨绿跌、灰平和深色画布；hover 只突出当前祖先链，未改变数据颜色含义。最大化遮罩使用中性黑与轻微 backdrop blur，不引入新品牌色。
- 图像与资产质量：热力图为 ECharts canvas，显式最高 2 倍 DPR；无截图放大、位图替代或 CSS 假图。最大化/退出复用项目现有 Lucide 图标，笔画与其他工具按钮一致。
- 文案与内容：保留行情/行业覆盖、来源、盘中状态、面积口径和色阶；新增“悬浮查看明细，点击查看个股”“最大化”“退出全屏（Esc）”，操作目的明确。

## 交互、可访问性与运行证据

- 最大化按钮为语义 button，具有 `aria-expanded` 和明确名称；最大化层为 `role=dialog + aria-modal`，焦点进入“退出全屏”，`Esc` 能退出并恢复 body 滚动。
- 普通和最大化切换会安全重建 ECharts 实例；ResizeObserver、流通市值/成交额/等权、tooltip、行业选择和个股预览回调不丢失。
- 新增最大化回归后，前端全量 29 个测试文件、113 项通过；目标 ESLint、TypeScript/Vite 生产构建通过。正式浏览器两种状态控制台 error 均为 0。

## Findings 与比较历史

- 用户反馈对应的 P1（热力图不在页面首位）和 P2（普通画布/文字偏小、缺少大视图入口）均已修复。
- 第 1 轮同尺寸首屏比较：位置、面积、层级和控制可见性通过，没有新的 P0/P1/P2。
- 第 2 轮 `2048×1024` 最大化悬浮比较：tooltip、聚焦暗化、边界和标签层级通过；按钮边界实测位于视口内，没有裁切。
- P3：全市场小市值个股不可能在单张矩形树图中全部显示名称；当前以面积省略、悬浮 tooltip 和最大化三层机制处理，不阻塞使用。

## Implementation checklist

- [x] 热力图移到行业分析首屏第一位
- [x] 普通画布增高、字体和 tooltip 清晰度提升
- [x] 标题栏最大化/退出全屏
- [x] `Esc`、焦点与背景滚动恢复
- [x] 1382 首屏、2048 最大化悬浮、1024 较窄桌面
- [x] 同尺寸对照、控制台、全量测试、构建与 lint

final result: passed
